{
  "name": "bt-core",
  "version": "2.0.0",
  "type": "module",
  "private": false,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "release:prep": "npm run build && git add dist && git commit -m \"build(core): dist for release\" || echo nochange"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}